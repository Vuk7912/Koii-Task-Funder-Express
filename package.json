{
    "name": "coingecko-mock-api",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "test": "vitest",
        "test:coverage": "vitest run --coverage"
    },
    "devDependencies": {
        "vitest": "latest",
        "@vitest/coverage-v8": "latest",
        "typescript": "latest"
    }
}