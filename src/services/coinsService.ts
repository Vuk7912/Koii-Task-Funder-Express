import mockCoinsData from '../data/mockCoins.json';

/**
 * Retrieves the list of coins
 * @returns {Array} List of coins with their details
 */
export function getCoinsList() {
  return mockCoinsData;
}